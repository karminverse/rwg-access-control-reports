# Collateral Integrity Checkup -- 2026-06-15

Baseline accepted 2026-06-08  |  Scanner trustfall-weekly @ 568c059  |  schema_version 2
Observed 8  |  changed 4  |  no-change 4  |  new 0  |  unreadable 0  |  excluded 1
Data integrity: clean (0 ERROR, 0 WARN across roster).

Verification: read-only on-chain claim-walk per changed asset + critical-lever
sweep. Every CHANGED / PENDING item carries an on-chain evidence stamp.

## TL;DR

Net: one item for the RWG to weigh -- **frxUSD's 250x custodian mint-cap raise**
(100k -> 25M, Safe-gated, confirmed on-chain). Everything else is lender-neutral
or no-change. reUSD is parked (migration, pending review).

- **frxUSD** -- custodian mintCap 100k -> 25M (250x, mint-inflation); confirmed @
  block 25290175. Marquee. Other moves REDUCE authority (operator + freezer removed).
- **USDe** -- 3 benefactors added (user-tier, not minters) + routine mint/redeem -> lender-neutral.
- **StakedUSDeV2** -- 33 end-user restriction adds (role-admin unchanged, FiRM not listed) + reward stream -> lender-neutral.
- **DOLA** -- Fed contraction (120k DOLA burned, supply-reducing) -> neutral.
- No-change (4): crvUSD, rewardVault, scrvUSD, sfrxUSD. Excluded (1): reUSD (parked).
- Pending: none new (DOLA ghost = acknowledged). Critical levers: all 4 in-roster declaring assets stable.

## No change (4)
crvUSD . rewardVault . scrvUSD . sfrxUSD

## Excluded (1)
reUSD -- parked: protocol migration in progress (RE-staking module + Chainlink
CCIP rollout), awaiting its review cycle. Not scanned or diffed this period.

## Changed (4)

One asset moved a harm-vector lever this period -- **frxUSD** (mint inflation).
The other three are lender-neutral.

### frxUSD -- touches: mint inflation

- **Custodian mint cap raised 250x.** setMintCap on FrxUSDCustodianWithReceiver
  0x860c...7857: mintCap 100,000 -> 25,000,000 frxUSD at block 25290175,
  executed via owner Safe 0xfFFf...3937.
  - verified: eth_call mintCap() == 25,000,000e18 now / 100,000e18 @25274514;
    MintCapSet @ block 25290175 (tx 0x0c93943a...). (eth_call + eth_getLogs, 2026-06-15)
- Other frxUSD moves REDUCE authority (lender-neutral / de-risking):
  - Operator 0x9032cce6... de-approved on BOTH custodians (0x4f95, 0x860c) in one
    tx @ block 25319375 (isApprovedOperator -> false on both, verified).
  - Delegated freezer 0x8d1ce21f... removed from the FrxUSD token (RemoveFreezer @
    block 25319391; isFreezer -> false). Retires a standalone freeze key; owner
    Safe retains intrinsic freeze. n_roles 49 -> 48.
  - deposit() -- routine user activity.

### USDe -- touches: none (lender-neutral)

- benefactor() +3 members (0x2e3d..., 0xae51..., 0xddcd...) via
  addWhitelistedBenefactor (BenefactorAdded @ blocks 25299070 / 25299351). These
  are KYC'd order-originators (user-tier), NOT MINTER_ROLE -- mint authority over
  USDe is unchanged (minter() still EthenaMinting V2; mint caps unchanged).
- mint()/redeem() set_at_block bumps = routine traffic (Mint @25324146, Redeem
  @25323126).
  - verified: eth_getLogs + eth_call (isWhitelistedBenefactor false@25274516 ->
    true now). (2026-06-15)

### StakedUSDeV2 -- touches: blacklisting (end-users only) -> lender-neutral

- FULL_RESTRICTED_STAKER_ROLE +33 members -- Ethena's end-user restriction list
  (33 RoleGranted, first @ block 25278833, granter = existing BLACKLIST_MANAGER
  EOA 0x49e9...55fe). Role admin unchanged (getRoleAdmin = DEFAULT_ADMIN at both
  window ends); FiRM's Safe/distributor are NOT on the list, so the freeze /
  seizure path cannot reach FiRM's sUSDe collateral.
- transferInRewards -- 21 RewardsReceived events (last @ block 25324249),
  yield-additive (raises sUSDe price-per-share).
  - verified: eth_getLogs + eth_call. (2026-06-15)

### DOLA -- touches: none (supply-reducing) -> lender-neutral

- Fed.contraction() fired @ block 25295073: 120,000 DOLA burned from market
  0xb516...330b via chair Safe 0x8F97...DfC8 (tx 0x64cd62...710d). Supply-REDUCING
  -> cannot impair collateral value; normal monetary op (no Expansion in-window).
  - verified: eth_getLogs Contraction @ block 25295073. (2026-06-15)

## Pending -- newly-queued critical-lever calls (0)

No newly-queued critical-lever timelock calls this period
(pending_lever_calls.added empty across the entire roster).

**Known exceptions (acknowledged, not action items):** DOLA changeMarketCeiling
ghost -- op_id 0x4bbb3e09... on Compound timelock 0x926df14a..., queued
2024-12-16, eta 2024-12-18, long past grace; confirmed NO Execute/Cancel
in-window (expired, never executed).

## Critical levers -- verified stable

The unbound value/pointer sweep ran on all 4 in-roster assets that declare
critical_parameters and found NO movement this period:

- **DOLA** -- live operator() pointer stable (Compound Timelock 0x926d..., both
  window ends); pendingOperator 0x0.
- **crvUSD** -- per-controller monetary_policy() unchanged across all 9
  controllers; collect_fees_above_ceiling ruled out indirectly (totalSupply flat);
  zero ControllerFactory / OwnershipProxy logs over the window.
- **rewardVault** -- setStrategy / setAllocator pointers (CurveStrategy /
  OnlyBoostAllocator) and impl bytecode codehash all byte-identical across the
  window; zero ProtocolController / Timelock logs.
- **scrvUSD** -- set_accountant and set_withdraw_limit_module both still 0x0 (no
  fee-assessor / redemption-gate installed); custody-flow inert (totalDebt 0).

---
trustfall-weekly @ 568c059 . 8 sidecars diffed vs accepted baseline 2026-06-08 .
verification: read-only on-chain claim-walk + critical-lever sweep across the roster.

reUSD parked as of 2026-06-15 (protocol migration; pending review), excluded
from this and subsequent runs.
