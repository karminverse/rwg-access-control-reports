# Collateral Integrity Checkup -- 2026-07-20

Baseline accepted 2026-07-13  |  Scanner trustfall-weekly @ 0d77cd4  |  schema_version 2
Observed 9  |  changed 6  |  no-change 3  |  new 0  |  unreadable 0
Data integrity: clean (0 ERROR across roster).

Verification: read-only on-chain claim-walk per changed asset + unbound critical-lever
sweep, then an adversarial refutation pass on the headline items. Every CHANGED item
carries an on-chain evidence stamp.

## TL;DR

Net: **two items to weigh.** (1) Ethena **completed** the governance migration flagged
last period -- USDe and sUSDe are now owned/administered by the 24h timelock, and the
5/10 Safe can no longer re-key the minter directly. Read the win precisely though: the
same Safe holds every role on that timelock, so the practical gain is a **24-hour public
warning period, not an independent veto**. (2) DOLA's newly onboarded SaveDola market
took another **+3.9M DOLA** and now sits at **98% of its 5M ceiling**. Nothing was
unauthorized; no collateral was impaired.

- **USDe** -- token `owner()` moved 5/10 Safe -> 24h timelock via the **delayed path**;
  `setMinter` from the Safe now reverts and is not whitelisted for instant execution.
  Closes last period's review window. (mint inflation -- de-risking, with residuals below)
- **StakedUSDeV2** -- sUSDe `owner()` + DEFAULT_ADMIN likewise moved to the timelock in
  the same batch; +34 end-user blacklist entries, zero FiRM addresses. Instant-blacklist
  bypass remains inert. (mint inflation / blacklisting -- de-risking + lender-neutral)
- **DOLA** -- Fed minted **+3,900,000 DOLA** into the SaveDola market (one call, ceiling
  never raised, chair/gov unchanged). Market now 4.9M drawn of a 5.0M cap; ~1.47M
  actually borrowed. (mint inflation -- authorized; watch the headroom)
- **sfrxUSD** -- yield rate raised **3.85% -> 4.10% APY** (+25 bps) by the Frax 3/6 gate;
  the larger book value move is a mandatory interest catch-up, not a repricing.
  (collateral valuation -- routine yield administration)
- **reUSD** -- routine daily NAV accrual (+0.1185%) and user deposits; setter identity
  unchanged. **frxUSD** -- routine user mint/deposit; mint cap verified flat at 25,000,000.
  Both lender-neutral.
- No-change (3): crvUSD . rewardVault . scrvUSD. Pending: none newly-queued.
  Critical levers (DOLA . crvUSD . rewardVault . scrvUSD . reUSD): all stable.

## No change (3)
crvUSD . rewardVault . scrvUSD

## Changed (6)

Harm-vector movers first, then the lender-neutral.

### USDe -- touches: mint inflation -> DE-RISKING (last period's review window CLOSED)
- **The USDe token's ownership transfer to the 24-hour timelock was accepted.** `owner()`
  moved from the Ethena 5/10 Safe to the timelock and `pendingOwner()` cleared to zero.
  (Precisely: the offer leg predated this window; only the **acceptance** occurred here.)
  It rode the **delayed path, not the instant bypass** -- acceptance was call #0 of a
  7-call batch scheduled by the Safe and executed ~5.25 days later against a 24h minimum,
  with all 7 executions matching the 7 scheduled calls and the operation recorded done.
- **The mint-rekey lever is genuinely closed to the Safe.** Simulated `setMinter` and
  `transferOwnership` from the 5/10 Safe now revert on the ownership check, while the same
  call from the timelock would succeed; `setMinter` is **not** on the timelock's zero-delay
  whitelist, and the token is not a whitelisted target for any selector. Widening that
  whitelist is itself a 24h-delayed action. `minter()` unchanged; no bytecode change on
  token or minting contract.
  - verified: eth_call owner()/pendingOwner() at both endpoints; OwnershipTransferred @
    block 25574214; schedule @ 25536572 (delay 86400) vs execute @ 25574214; operation-id
    recomputed on-chain from the decoded batch; whitelist re-enumerated live (12 entries,
    0 removals, unchanged this period); revert-simulation of setMinter from both callers.
    (eth_call + eth_getLogs, adversarially re-verified, 2026-07-20)
- **How far to read the win -- three standing residuals (pre-existing, not new this period):**
  1. **The delay is a warning period, not a veto.** The same 5/10 Safe holds *proposer,
     canceller, executor and whitelisted-executor* on the timelock (all granted at its
     deployment). It can propose and execute alone, and is the sole canceller. Ultimate
     authority did not change hands -- what changed is that supply-authority moves are now
     publicly visible for 24 hours first.
  2. **Minting itself is still instant.** The hardening governs *who may become* minter.
     The operational MINTER_ROLE is held by 20 externally-owned accounts that mint
     immediately within per-block caps (200M USDe/block globally).
  3. **A large zero-delay lever remains outside the timelock's delay.** The Safe can
     instantly re-point the cross-chain adapter that custodies **~1.49B USDe (about 37% of
     supply)**, and that adapter carries **no rate limiter**; it can also instantly disable
     mint/redeem (re-enabling takes the 24h path). Neither touches total supply, so neither
     is a mint-inflation vector -- but they bound how much comfort to take from "USDe is
     behind a timelock." Worth a standing watch item.
- Routine: `benefactor()` +14 (user-tier; all verified to hold no minter/redeemer/admin
  role), added via the intended zero-delay benefactor path; mint/redeem activity markers.

### StakedUSDeV2 -- touches: mint inflation (governance) + blacklisting -> DE-RISKING + lender-neutral
- **sUSDe governance migrated in the same batch:** `owner()` and DEFAULT_ADMIN_ROLE both
  moved from the 5/10 Safe to the 24h timelock, via the delayed path (the sUSDe leg was a
  bare `acceptAdmin()` in the same 7-call operation). The standing "no timelock in admin
  chain" finding on this asset is resolved. The residuals above apply equally here.
  - verified: eth_call owner()/hasRole(DEFAULT_ADMIN,..) flipped at both endpoints;
    AdminTransferred + RoleRevoked(Safe) + RoleGranted(timelock) in the same transaction @
    block 25574214. (eth_call + eth_getLogs, 2026-07-20)
- **Blacklist +34 end-user wallets** (blocks 25525061-25573793, all from the existing
  blacklist-manager key, all predating the migration). No FiRM market or escrow restricted;
  role admin unchanged.
- **The instant-blacklist bypass remains inert.** The 5/10 Safe still holds
  BLACKLIST_MANAGER_ROLE directly and the timelock does not, so the whitelisted zero-delay
  blacklist entry cannot currently be used -- confirmed by simulating the call, which
  reverts. Unchanged from last period.

### DOLA -- touches: mint inflation (authorized) -> informational, watch the headroom
- **The Fed minted +3,900,000 DOLA in a single call** (2026-07-13, hours after last
  period's baseline) into the market collateralized by the SaveDola token. The figure
  reconciles wei-exact three ways: Fed global supply, that market's supply line, and DOLA's
  total token supply each rose by exactly 3,900,000 -- this one call is the entire weekly
  DOLA supply increase. Zero contractions anywhere in the window.
- **Nothing was unlocked to permit it.** The market's ceiling stood at 5,000,000 before, at,
  and after the mint -- never raised. Chair and governor are unchanged, and the governance
  timelock executed nothing at all this period (19 queued, 0 executed, 0 cancelled). The
  mint used the chair's normal, chair-only path.
- **Trajectory is the analyst-relevant part.** This market was empty two periods ago, took
  1,000,000 last period and 3,900,000 now: **4.9M drawn against a 5.0M cap = 98% consumed,
  ~100,000 DOLA of headroom.** Only ~1.47M is actually borrowed, leaving ~3.43M of minted
  DOLA sitting undrawn in the market. Further growth here requires a governance ceiling
  raise -- which would appear as a queued timelock call.
  - verified: eth_getLogs 1 Expansion / 0 Contractions (block 25525641, amount exact);
    eth_call global supply, market supply, token total supply, and ceiling at both
    endpoints; chair/gov identical. Adversarially re-derived wei-exact. (2026-07-20)

### sfrxUSD -- touches: collateral valuation (routine) -> lender-neutral
- **The yield-accrual rate was re-set: 3.85% -> 4.10% APY (+25 bps)**, the first change to
  this rate since block 25.19M. Set by sfrxUSD's expected authority gate (a Frax 3-of-6
  multisig; note the gate is *named* like a timelock but is a Safe), whose address, owner
  set and threshold are identical at both window endpoints.
- **The larger-looking book-value move is not a second event.** Raising the rate forces an
  internal sync that writes ~49 days of already-earned interest into the stored checkpoint,
  which is why stored price-per-share jumped +0.51%. Replaying the *old* rate across that
  exact interval reproduces the new stored value to the wei. What integrators actually
  observe moved **+0.075% over the seven-day window (~4.0% annualized)** -- no
  discontinuity, no repricing. No cap or band is declared for this rate in the asset's
  profile; the observed use is ordinary-direction, modest-magnitude yield administration.
  - verified: eth_call both rate and price fields at both endpoints; single rate-set event
    @ block 25547992 with caller confirmed as the unchanged gate; sync arithmetic
    independently reproduced; implementation bytecode identical. (2026-07-20)

### reUSD -- touches: none (lender-neutral)
- Routine daily NAV accrual: share price +0.1185% via **7 gapless daily updates** of
  +1.69 bps each, ~24h apart -- same slope as last period (the 7-vs-6 count is just the
  window catching one extra tick). Setter identity intact three ways: the price-setter role
  still resolves to the NAV consumer contract, zero role-change events in the window, and
  every push arrived through the same automation entrypoint with no manual override.
  Remaining deltas are routine user deposits and staking.
  (verified eth_call + eth_getLogs, 2026-07-20)

### frxUSD -- touches: none (lender-neutral)
- Routine user activity on the USDC custodian: an exact-shares mint (the first use of that
  entrypoint since block 24.75M -- dormancy, not privilege) and a deposit. Both are public
  ERC-4626 entrypoints, not privileged setters.
- **The mint-cap scare resolved clean:** the custodian mint cap reads **25,000,000 frxUSD
  at both window endpoints, identical to the wei**, with no cap-setting event anywhere in
  the window. No mint-inflation movement.
  (verified eth_call + eth_getLogs, 2026-07-20)

## Pending -- newly-queued critical-lever calls (0 new)

No newly-queued critical-lever timelock calls this period. The Ethena timelock's only
in-window operation is the migration batch described above, and it has executed -- no
operation remains pending there. Note for context: DOLA's governance timelock holds 19
queued transactions and executed none this period.

## Critical levers -- verified stable

The unbound value/pointer lever sweep ran on all 5 in-roster declaring assets -- no
movement this period:
- **DOLA** -- operator pointer and pending-operator steady; the exercised expansion is a
  bound lever, already in Changed.
- **crvUSD** -- monetary-policy pointers byte-identical across all 9 controllers; zero
  setter events; total supply flat (indirect proof for the fee-mint lever, which has
  neither event nor getter).
- **rewardVault** -- strategy and allocator pointers identical at both endpoints; root-vault
  codehash byte-identical; component-setter events zero across the full window.
- **scrvUSD** -- accountant and withdraw-limit-module both still zero-address; custody flow
  inert.
- **reUSD** -- all unbound value/pointer levers stable, each closed by both a setter-event
  sweep and two-endpoint getter reads.

---
trustfall-weekly @ 0d77cd4 . 9 sidecars diffed vs accepted baseline 2026-07-13 .
verification: 11 read-only claim-walk agents + 5-agent adversarial refutation pass
(cast eth_call / eth_getLogs / call simulation). Toolchain: pip-audit clean.
