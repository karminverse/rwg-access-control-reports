# Collateral Integrity Checkup -- 2026-07-13

Baseline accepted 2026-07-07  |  Scanner trustfall-weekly @ 61eec4a  |  schema_version 2
Observed 9  |  changed 5  |  no-change 4  |  new 0  |  unreadable 0
Data integrity: clean (0 ERROR across roster).

Verification: read-only on-chain claim-walk per changed asset + unbound
critical-lever sweep, then an adversarial refutation pass on the headline items.
Every CHANGED item carries an on-chain evidence stamp.

## TL;DR

Net: **two real supply-side items to weigh -- both governance-authorized, neither
an authority compromise, but material.** (1) Ethena began moving USDe's mint-authority
governance to a new 24-hour timelock -- but the migration is **incomplete**, so the
5/10 Safe still holds an instant mint-rekey lever this week. (2) The Inverse Fed
expanded **+6.6M DOLA** into FiRM markets via the normal, governance-onboarded
mechanism. No existing collateral was impaired; no unauthorized authority moved.

- **USDe** -- EthenaMinting's admin/owner moved to a new 24h TimelockController, BUT
  the USDe *token's* ownership transfer is **initiated and not yet accepted** -- the
  5/10 Safe remains token owner and `setMinter` is owner-only + instant. Directionally
  de-risking once completed; **in-flight** this week. (mint inflation / governance)
- **DOLA** -- Fed expansion **+6.6M DOLA** (3 draws incl. a seed for a newly onboarded
  SaveDola market); Fed global supply 87.08M -> 93.68M; chair/gov/operator unchanged.
  Authorized supply expansion within governance-set ceilings. (mint inflation)
- **StakedUSDeV2** -- FULL_RESTRICTED_STAKER_ROLE +45, all end-user wallets, zero FiRM
  address restricted -> lender-neutral. (blacklisting)
- **reUSD** -- first checkup since un-parking: routine NAV accrual (+0.10%) and user
  deposits; authorized share-price setter unchanged -> lender-neutral.
- **frxUSD** -- one routine custodian deposit; mint cap, fees, and proxy impl all
  verified flat -> lender-neutral.
- No-change (4): crvUSD . scrvUSD . rewardVault . sfrxUSD. Pending: none newly-queued
  (but see USDe's in-flight ownership acceptance -- live review window). Critical
  levers (DOLA . crvUSD . rewardVault . scrvUSD . reUSD): all stable.

## No change (4)
crvUSD . scrvUSD . rewardVault . sfrxUSD

## Changed (5)

Harm-vector movers first, then the lender-neutral.

### USDe -- touches: mint inflation (governance chain) -> IN-FLIGHT, watch
- **Ethena is migrating USDe's mint-authority governance to a new 24-hour timelock --
  and it is not finished.** What HAS moved: `EthenaMinting` owner / admin /
  DEFAULT_ADMIN_ROLE transferred from the Ethena 5/10 Safe to a new custom
  TimelockController (real 24h `getMinDelay`, self-administered, the 5/10 Safe as sole
  proposer and sole canceller; op ledger 17 scheduled / 17 executed / 0 cancelled).
  What has NOT moved: the **USDe token itself is still owned by the 5/10 Safe** --
  ownership transfer to the timelock is *initiated but unaccepted* (2-step). Because
  `setMinter` on the token is owner-only and instant, the Safe retains a direct,
  timelock-free path to re-key the minter and mint without cap. **So the top supply
  lever is NOT yet de-risked** -- the net posture this week is unchanged; the hardening
  lands only when the token ownership acceptance executes.
  - verified: eth_call owner()/hasRole(DEFAULT_ADMIN) on EthenaMinting flipped
    Safe -> timelock @ block 25522488; token owner() still = Safe, pendingOwner() =
    timelock (OwnershipTransferStarted @ 25522472), acceptOwnership not yet called;
    getMinDelay()=86400; minter() unchanged. Adversarially re-verified (full role-event
    enumeration, no missed revokes/cancels). (eth_call + eth_getLogs, 2026-07-13)
- **Two bypass powers to note on the new timelock:** the 5/10 Safe holds a zero-delay
  `executeWhitelisted` path over a 12-entry whitelist. Its contents are defensive/ops
  in character (the `disableMintRedeem` emergency brake; user-tier benefactor
  onboarding) BUT also include **instant `setPeer` on the USDe / sUSDe / ENA
  cross-chain (LayerZero) tokens** -- a bridge-reconfiguration lever -- and instant
  `addToBlacklist` on sUSDe / sENA. Whitelist additions are themselves 24h-timelocked;
  no bypass has been exercised to date.
- Routine: benefactor roster +4 (user-tier KYC, none hold MINTER_ROLE).
- **Migration scope (cross-asset):** this is a *partial* Ethena migration -- it moved
  EthenaMinting's governance only. StakedUSDeV2's token admin/owner and the staking
  reward distributor remain on the 5/10 Safe. Do not read this as "Ethena governance
  fully behind a timelock."

### DOLA -- touches: mint inflation (authorized expansion) -> informational
- **The Fed minted +6,600,000 DOLA into FiRM markets this period** -- the expansion
  lever, dormant since ~block 25.14M, was exercised three times: +600,000 into the
  sUSDe market (block 25482157), and +5,000,000 into the yvCurve-DOLA-sUSDe-f market
  plus +1,000,000 seeding a newly onboarded **SaveDola** market (both block 25523296).
  The new market was onboarded by a governance vote (ceiling 0 -> 5,000,000, registered
  in DBR). Fed global supply moved 87,084,203 -> 93,684,203; per-market supplies
  reconcile exactly. This is the designed, governance-gated DOLA issuance mechanism --
  not an authority change: chair, governor, and the market-ceiling operator are all
  unchanged. (Note: the new market's "SaveDola" collateral is a distinct token from
  canonical sDOLA.)
  - verified: eth_getLogs 3 Expansion / 0 Contraction events (amounts + markets +
    blocks re-derived independently); eth_call global supply and ceilings(new market)
    at both window endpoints; chair()/gov unchanged. Adversarially re-derived --
    +6.6M reproduced exactly. (eth_call + eth_getLogs, 2026-07-13)

### StakedUSDeV2 -- touches: blacklisting (end-user only) -> lender-neutral
- FULL_RESTRICTED_STAKER_ROLE +45 members (blocks 25479040-25524171), granted by the
  existing blacklist-manager key. All 45 are end-user wallets (35 EOAs + 10 clones of a
  mass-deployed sweeper/forwarder with distinct owners); none is a FiRM market or escrow.
  Role admin unchanged; no new blacklisting authority; confiscation not exercised.
  - verified: eth_getLogs 45 RoleGranted / 0 RoleRevoked; hasRole state-diff on a
    sample; getRoleAdmin unchanged. (eth_getLogs + eth_call, 2026-07-13)
- **Related to the USDe item:** the new Ethena timelock's whitelist grants the 5/10
  Safe *instant* `addToBlacklist` on sUSDe/sENA. It is real but currently **inert** --
  the timelock does not yet hold BLACKLIST_MANAGER_ROLE (the Safe holds it directly),
  so the whitelisted path is dormant until that manager role migrates.

### reUSD -- touches: none (lender-neutral)  [first checkup since un-parking]
- Routine post-migration operation, all four deltas genuine and benign: the global
  share price rose +0.1015% over the week via six daily `SharePriceSet` updates of
  +1.69 bps each (a gapless chain matching both scanned endpoints), driven by the
  authorized PRICE_SETTER_ROLE holder (NAVConsumer, unchanged); plus routine
  InsuranceCapitalLayer deposits and a staking deposit.
  - verified: eth_call getSharePrice() at both endpoints; eth_getLogs 6 SharePriceSet
    events; role holder unchanged. (eth_call + eth_getLogs, 2026-07-13)

### frxUSD -- touches: none (lender-neutral)
- One routine ERC-4626 deposit on the USDC custodian near the scan head. All harm
  vectors verified untouched: mintCap flat at 400,000,000, mintFee/redeemFee both 0,
  EIP-1967 impl + admin slots byte-identical, zero authority events on token or
  custodians. (verified eth_call + eth_getLogs, 2026-07-13)

## Pending -- newly-queued critical-lever calls (0 new)

No newly-queued critical-lever timelock calls this period.

**Live review window (forward-looking, not a timelock queue op):** the USDe token's
2-step ownership transfer to the new Ethena timelock is initiated but unaccepted --
owner() is still the 5/10 Safe. Expect the owner() flip in a coming period when
`acceptOwnership` executes; that is the event that actually hardens the token's
`setMinter` lever. Inspect that acceptance when it lands.

## Critical levers -- verified stable

The unbound value/pointer lever sweep ran on all 5 in-roster declaring assets --
no movement this period:
- **DOLA** -- operator() pointer steady (governance timelock); the exercised expansion
  and market-ceiling raise are bound levers, already in Changed.
- **crvUSD** -- set_monetary_policy pointers byte-identical across all 9 controllers;
  zero SetMonetaryPolicy events; totalSupply flat (rules out a net fee mint).
- **rewardVault** -- setStrategy / setAllocator pointers identical; root-vault codehash
  byte-identical; zero ProtocolController events in-window.
- **scrvUSD** -- set_accountant and set_withdraw_limit_module both 0x0 at both
  endpoints; custody-flow inert.
- **reUSD** -- first sweep: 7 unbound value/pointer levers all stable (getters identical
  at both endpoints, zero setter events).

---
trustfall-weekly @ 61eec4a . 9 sidecars diffed vs accepted baseline 2026-07-07 .
verification: 12 read-only claim-walk agents + 5-agent adversarial refutation pass
(cast eth_call / eth_getLogs). Toolchain: pip-audit clean.
