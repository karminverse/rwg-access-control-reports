# Collateral Integrity Checkup: 2026-09-14

Baseline accepted 2026-09-07  |  Scanner trustfall-weekly @ e7ba11f  |  schema_version 2
Observed 11  |  changed 8  |  no-change 3  |  new 0  |  unreadable 0  |  excluded 0

**Data integrity:** clean (0 ERROR, 10 WARN across the roster, 0 missed blocks).

**Verification:** read-only on-chain claim-walk per changed asset, direct reads of every queued
operation carried into the week, an unbound critical-lever sweep, and a governance-timelock queue
sweep across five surfaces, followed by adversarial passes on material items and a critic over the
draft.

## TL;DR

**Net: an authority-heavy week. The re.xyz reUSD admin handover landed, and the same day a single
key gained instant pause over every senior exit. The DBR rate was raised outside governance for a
second week. A Stake DAO batch will replace the custody code behind a live FiRM market on
2026-09-16.**

1. **re.xyz reUSD: the operator's admin moved behind a 2-day timelock on all 12 contracts, and 17
   hours later a single key gained instant pause and unpause over all three senior exit
   contracts.** The handover is delivered. The pause grant was made directly by the operator
   multisig as AccessManager root admin, with no timelock, to a plain key with zero execution
   delay. Before, only the multisig could pause these contracts. The AccessManager half of the
   stack is still not behind the timelock: the multisig's removal as its root admin has been
   executable since 2026-09-11 and remains unexecuted. The asset is in assessment with no live
   FiRM market, so read this as suitability input.
2. **The DBR emission rate rose 47M to 51M per year, outside any proposal, for the second week
   running.** Neither in-window proposal carries it; the Fed chair multisig set it directly.
   Direction: a higher rate makes borrowing cheaper, not dearer.
3. **A Stake DAO batch executable from 2026-09-16 08:45 UTC replaces the custody code behind FiRM's
   SaveDola market**, whose borrower debt rose about 395k this week to about 4.44M DOLA. Once it
   executes, anyone can trigger its new migration module, which can shift that LP between Stake
   DAO's locker and a new Convex sidecar but never to an outside address. The new allocator also
   drops the market's locker-only setting.

**By asset**

- **re.xyz reUSD** (forced pause, proxy upgrade) **WATCH**
  Admin handover delivered on 12 of 12 contracts. Same day: a new single-key pause over every
  senior exit, emergency powers moved into a role only the multisig holds, and the multisig's
  AccessManager root-admin removal queued but unexecuted.
- **DOLA** (fee manipulation) **WATCH**
  DBR rate raised 47M to 51M outside governance. Proposal 374 raised the governance quorum and
  proposal threshold. Supply flat.
- **rewardVault** (proxy upgrade) **WATCH**
  Six queued calls on this asset's levers belong to the Stake DAO custody-code batch above.
- **Resupply reUSD** (mint inflation) **WATCH**
  Proposal 29 executed: the sfrxUSD ramp retargeted to 40M with a 2.7x faster slope, and an older
  sfrxUSD market cut to one wei. Three ceilings advanced on the keeper.
- **USDe** (mint inflation) **LENDER-NEUTRAL**
  Supply +5.35% through the single minter. 13 benefactors added. Bridge routes reshaped under the
  timelock.
- **frxUSD** (mint inflation) **LENDER-NEUTRAL**
  Supply fell 5.61M on custodian redemptions; the AUSD-backed custodian made its first mint, 1,000
  frxUSD.
- **StakedUSDeV2 (sUSDe)** (no vector touched) **LENDER-NEUTRAL**
  Routine reward flow of 1.16M USDe.
- **Monolith invUSD** (no vector reaches DOLA) **LENDER-NEUTRAL**
  Only delta is the proposal 374 governance change it shares with DOLA.

**No change (3):** crvUSD, scrvUSD, sfrxUSD
**Pending:** the Stake DAO custody batch; three re.xyz reUSD operations, including the root-admin removal
**Critical levers:** 29 unbound levers swept, one moved and is reported below

## No change (3)

crvUSD, scrvUSD, sfrxUSD

## Changed (8)

### re.xyz reUSD | forced pause, proxy upgrade | WATCH

**Two changes on 2026-09-09, 17 hours apart, moved this asset's live authority. The first narrowed
it. The second widened it in one place.**

**DELIVERED: the operator's DEFAULT_ADMIN moved behind the 2-day timelock on all 12 contracts.** A
twelve-call batch scheduled on 2026-09-02 executed at block 25,936,909, 02:44 UTC. The operator
multisig's DEFAULT_ADMIN reads true at the window's open and false now on all twelve targets across
both tranches, and the 2-day timelock holds it on each. Upgrade authority on both insurance capital
layers moved with it.

- **What the operator retains, at zero delay:** pause authority on the insurance capital layers,
  deposit-token registries and prestaking contracts; admin roles on the registries and prestaking;
  and the emergency updater role on both NAV consumers, which is the live NAV write hand, so the
  share price stays reachable at zero delay.
- **What the handover is worth:** the operator's admin was not removed, it went from instant to
  2-day-delayed and publicly visible. The multisig holds proposer, executor and canceller on that
  timelock, which administers itself, so it can re-grant its own admin with two days of on-chain
  notice.

**ADDED, 20:03 UTC the same day: a single key gained instant pause and unpause over all three senior
exit contracts.** In a separate transaction at block 25,942,082, the multisig acted directly as
AccessManager root admin, with no timelock and no scheduling. It created a new role, labelled
"eth.pause", gating pause and unpause on RedemptionGateway, InstantRedemption and the senior
WindowRedemption, and granted it to itself and to plain EOA 0x07e5fac5. Both the execution delay and
the grant delay are zero, so the power was live on the grant.

- Pausing the three together stops every on-chain exit from the senior token: instant redemption and
  the whole window-request lifecycle of submit, adjust, cancel and claim. The same key can also
  unpause a contract the multisig has paused.
- Before this, only the multisig's root-admin seat could call pause on these contracts. The grant is
  additive: the multisig keeps pause through the same role.
- This is the only role that key gained in the window, and the key has never sent a transaction.
- Not exercised: no pause events, and all three contracts read unpaused at the window's close.

**Also in that transaction:**

- **The timelock was added as AccessManager root admin alongside the multisig, not in place of it.**
  The multisig still holds root admin at zero delay. Its own removal was scheduled on the timelock in
  the same transaction, became executable on 2026-09-11, and is still unexecuted. Until it runs, the
  AccessManager half of the stack, covering the redemption, payout, pricing and staking contracts,
  is not behind the timelock.
- **The multisig moved its emergency powers into a new role that only it holds**, which the pending
  removal does not touch: emergencyWithdraw on both redemption vaults, including the one funding
  instant redemptions, and forceNAVUpdate, applyMarkdown and the protection-mode controls on two new
  NAV oracles. Those oracles do not yet set the token's share price.
- **A 2-day admin delay was scheduled on 16 AccessManager targets, up from zero**, taking effect at
  20:03 UTC today, just after the window closed. That is hardening. RedemptionGateway is not among
  them, and the delay does not slow the pause role, since pausing is a role call rather than an
  admin operation.

**QUEUED, NOT DELIVERED:** the multisig's AccessManager root-admin removal (above); a fourth
single-key KYC provider, executable and unexecuted; and a staking vote-sync role for a new key,
activating 2026-09-16. The last two touch none of the five vectors.

**What it does not cover:** the live NAV write hand did not move. The share price rose 10.9 basis
points on its routine daily updates.

**What to watch:** execution or cancellation of the AccessManager root-admin removal; any pause
event on the three exit contracts; and any price-setter grant to the new NAV oracles, which would
make the multisig's instant markdown power live on the share price.

re.xyz reUSD remains in assessment with no live FiRM market. As suitability input: after this week,
one key can freeze every on-chain exit from the senior token without a quorum.

> Verified: DEFAULT_ADMIN read at the window's open and close on all 12 targets for both the
> multisig and the timelock; the handover batch's 12 execution events decoded from one transaction.
> AccessManager events in the 20:03 transaction decoded: role grants, function-to-role remaps and
> admin-delay updates, with role membership, execution delay and grant delay read at both
> endpoints. Pause state read on all three exit contracts. (2026-09-14)

### DOLA | fee manipulation | WATCH

- **The DBR emission rate rose 47,000,000 to 51,000,000 per year (+8.51%), outside any proposal, for
  the second week running.** The Fed chair multisig set it directly at block 25,935,590. Neither
  in-window proposal carries it: proposal 374 touches only governance parameters and proposal 375 is
  one transfer. The setter is reachable by the chair without a proposal or delay. Direction is
  counter-intuitive: the auction is a constant-product curve, so a higher emission rate lowers the
  DOLA price of the borrowing right. It follows a move from 45M to 47M the week before.
- **Proposal 374 raised the governance quorum from 15,500 to 24,000 INV and the proposal threshold
  from 1,900 to 7,270 INV**, executed at block 25,973,231 through the 2-day timelock. That hardens
  the body that operates DOLA's mint authority.
- **DOLA supply did not move**, 103,868,118.24 at both endpoints. The chair's single transaction this
  window was the rate change, not a mint.
- **Proposal 372 expired and proposal 375 paid it instead.** 372 lapsed unexecuted at block
  25,959,553; 375 sent the same 25,836 DOLA contributor payment as a direct transfer and executed.
  Treasury spend, no mint, lender-neutral.

> Verified: dbrRatePerYear read at the window's open and close; the chair's transaction decoded; both
> in-window proposals' action lists read. Quorum and proposal threshold read at both endpoints. DOLA
> totalSupply identical at both endpoints. (2026-09-14)

### rewardVault | proxy upgrade | WATCH

- **A Stake DAO timelock batch scheduled this window replaces the custody code for every Curve vault
  it manages, including the one behind FiRM's SaveDola market.** Six of its calls land on this
  asset's declared levers. It swaps the Curve strategy and allocator, three gateway modules, the
  registrars and the reward-mint approvals, revoking each incumbent in the same batch, and alongside
  those swaps it adds three permission grants, one of which lets the new migration module withdraw
  from sidecars. It is pending behind the 5-day delay and executes no earlier than 2026-09-16 08:45
  UTC.
- **Why it matters to FiRM:** borrower debt on market 0xc45e9c23 rose 394,564 DOLA this window, 9.8%,
  to 4,435,141.59, drawn from DOLA already supplied to the market. That debt is secured by the
  SaveDola LP, custodied almost entirely inside one Stake DAO vault. Once the batch executes, anyone
  can call the new migration module, which can shift that LP between Stake DAO's locker and a new
  Convex sidecar, never to an outside address. The new allocator is not set to locker-only for the
  SaveDola gauge, where the old one was, and nothing in the batch sets it; today's allocation keeps
  100% in the locker. The new code was deployed in this window by a key that holds no role in it and
  does not sign for the proposing multisig.
- The batch also shuts down a legacy sUSD-pool gauge whose vault holds nothing, so nothing is stranded
  by that call.
- This roster vault itself carries no live FiRM exposure; the lender weight is on the SaveDola vault.

> Verified: batch state read at both endpoints (created in-window, pending, neither executed nor
> cancelled); the protocol controller emitted no events in-window; the migration module's entry point
> and allocator settings read; SaveDola market debt and idle DOLA read at both endpoints; no code at
> the window's open for any contract the batch installs. (2026-09-14)

### Resupply reUSD | mint inflation | WATCH

- **Proposal 29 passed and executed at block 25,961,434**, after a full seven-day vote with no
  opposition. It retargeted the crvUSD/sfrxUSD ramp to 40,000,000 by 2026-10-01 and cut the older
  CurveLend crvUSD/sfrxUSD market from 50,000,000 to one wei. The retarget reset the ramp from the
  then-current 18,886,600, raising the slope 2.7 times to about 1,143,350 of ceiling per day, with no
  further vote needed until it reaches 40M. The older market's 6.28M of existing debt still repays
  normally.
- **Three ceilings advanced on the permissionless keeper:** crvUSD/sfrxUSD 16,986,600 to
  20,843,812.18, now past its old 20M target because of the retarget; crvUSD/syrupUSDC 12,529,749.54
  to 15,386,779.78; and crvUSD/sDOLA 16,338,683.04 to 19,674,622.64, due to reach its 20M terminal
  about six hours after the window closed.
- **Draw varied widely.** crvUSD/sfrxUSD took 1,728,194.71 of new debt, 44.8% of its new room.
  crvUSD/sDOLA took 18.8% and crvUSD/syrupUSDC 0.3%.
- **A new key gained a privileged role on a new incentives contract.** It was deployed on 2026-09-12
  by a fresh key that made itself the approved caller for claiming and routing governance-token
  emissions, and used it within 11 hours. That key was funded by the address that executed proposal
  29. The operations multisig then connected the contract to the emission stream through a direct
  claimer grant, outside any proposal and with no delay. The key can trigger claims at any time and
  chooses the gauges and split, but the governance token can only be deposited as vote-market
  incentives, never paid to itself. None of the five vectors.

> Verified: borrow limits read at both endpoints on all four pairs; ramp state read from the limit
> controller; proposal 29 lifecycle and execution transaction decoded; per-pair debt read at both
> endpoints. (2026-09-14)

### USDe | mint inflation | LENDER-NEUTRAL

- **Supply rose 234,565,867 (+5.35%) to 4,621,288,251.79 and reconciles to the wei** through the
  single authorised minter: 301,982,509.68 minted across 729 orders against 67,416,642.64 redeemed
  across 134. No minter, cap or owner changed.
- **13 new benefactors were whitelisted on the minting contract (582 to 595), none removed.**
  Benefactors are counterparties to orders; they cannot mint, redeem or move caps themselves.
- **Ethena reshaped the bridge routes on its USDe and sUSDe adapters.** Through its 1-day timelock it
  re-opened two routes that had been closed, raised a third 10,000-fold from 1,000 to 10,000,000 per
  hour, and trusted three new remote peers. Through the zero-delay whitelist lane it disconnected one
  route entirely, and a timelocked operation closed another. The adapters lock and release tokens;
  they cannot mint on Ethereum. Every opening sits behind the timelock and a 4-of-4 verifier set.
- An operation scheduled on 2026-08-21 executed, adding 21 zero-delay whitelist entries naming a BUIDL
  payment module. They cannot fire yet, because the timelock holds no authority on that module.

> Verified: totalSupply at both endpoints with a full zero-address transfer reconciliation;
> benefactor membership enumerated at both endpoints; adapter peers and rate limits read at both
> endpoints; scheduled and executed timelock operations decoded. (2026-09-14)

### frxUSD | mint inflation | LENDER-NEUTRAL

- **Supply fell 5,611,486.27 on custodian redemptions.** The oracle-priced custodian burned
  7,991,564.17; the USDC custodian was roughly flat, minting 2,918,814.16 and burning 2,928,261.60;
  and the cross-chain adapter netted a mint of 2,388,525.34.
- **The AUSD-backed custodian made its first mint**, 1,000 frxUSD. Its cap and holders are unchanged.
- No governance action on the token, its minters or custodians in the window. One peripheral
  contract did move: a Frax cross-chain RemoteHop was paused at block 25,934,673. Lender-neutral.

> Verified: frxUSD minter mint and burn events summed by minter, reconciling exactly to the
> totalSupply change; custodian caps unchanged at both endpoints; the RemoteHop paused state read
> before and after block 25,934,673. (2026-09-14)

### StakedUSDeV2 (sUSDe) | no vector touched | LENDER-NEUTRAL

- **20 reward injections totalling 1,157,012.44 USDe**, adding backing without minting shares. The
  per-injection size rose from 56,881.00 to 58,643.95 partway through the week.
- The sUSDe bridge adapter's route changes are covered under USDe.

> Verified: reward receipt events on the vault summed across the window. (2026-09-14)

### Monolith invUSD | no vector reaches DOLA | LENDER-NEUTRAL

- **Its only delta is the proposal 374 governance change**, quorum 15,500 to 24,000 INV and threshold
  1,900 to 7,270 INV, which applies because Monolith is governed by the same Inverse governor as DOLA.

> Verified: proposal threshold read at both endpoints. (2026-09-14)

## Pending: queued and matured calls

- **Stake DAO custody-code batch**, executable from 2026-09-16 08:45 UTC, replacing the code behind
  FiRM's SaveDola market (see rewardVault).
- **re.xyz reUSD, three operations:** the multisig's AccessManager root-admin removal, executable
  since 2026-09-11 and unexecuted; a fourth single-key KYC provider, executable and unexecuted; and a
  staking vote-sync role for a new key, activating 2026-09-16.

## Critical levers: swept, one moved

Sixty declared levers across seven declaring assets, of which 31 are bound to a tracked parameter and
already covered by the change-set. This sweep covered the 29 unbound ones. DOLA is bound on all four
of its levers.

- **One lever moved:** the re.xyz reUSD AccessManager meta-admin lever, through the role grants and
  function remaps reported above.
- **Verified stable:** Resupply reUSD 8, crvUSD 5, re.xyz reUSD's remaining 6, rewardVault 3, scrvUSD 3,
  Monolith invUSD 3.

---

trustfall-weekly @ e7ba11f  |  11 sidecars diffed against the accepted baseline of 2026-09-07
Verification: 6 claim-walk agents, 5 governance-surface sweeps, 6 adversarial passes and 1 critic.
Toolchain audit clean.
