# Collateral Integrity Checkup -- 2026-06-22

Baseline accepted 2026-06-15  |  Scanner trustfall-weekly @ 25d4c93  |  schema_version 2
Observed 8  |  changed 4  |  no-change 4  |  new 0  |  unreadable 0  |  excluded 1
Data integrity: clean (0 ERROR, 0 WARN across roster). crvUSD's warm pass hit a
transient dRPC missed-block range on the Curve Ownership Agent; it was re-scanned
COLD solo (audit CLEAN, full coverage) before this diff, so its no-change verdict
is gap-free.

Verification: read-only on-chain claim-walk per changed asset + critical-lever
sweep (8 agents). Every CHANGED / PENDING item carries an on-chain evidence stamp.

## TL;DR

Net: one item to note -- DOLA has a **newly-queued market wind-down** on market
0xb516...330b (`changeMarketCeiling -> 0` + `pauseBorrows(true)`, both pending,
callable 2026-06-24). It is **risk-REDUCING** (caps new DOLA borrows on that
market at zero), not a threat. Everything else is lender-neutral or no-change --
no mint-cap raises, no new mint paths, no role-admin changes this week.

- **DOLA** -- sole delta is the new pending wind-down op pair (see Pending);
  existing ceiling (500k DOLA) unchanged until it executes. Continues last week's
  120k-DOLA contraction on the same market.
- **StakedUSDeV2** -- +34 end-user restriction-list entries (role-admin unchanged,
  FiRM not listed) + ~54.4k USDe reward injection -> lender-neutral.
- **USDe** -- +1 KYC benefactor (user-tier, not a minter) + routine mint/redeem ->
  lender-neutral.
- **frxUSD** -- routine user deposits only; last week's 250x mintCap raise did NOT
  move again (flat at 25M) -> lender-neutral.
- No-change (4): crvUSD (cold-reverified) . rewardVault . scrvUSD . sfrxUSD.
  Excluded (1): reUSD (parked).
- Critical levers: all unbound value/pointer levers stable across the 4 in-roster
  declaring assets.

## No change (4)
crvUSD . rewardVault . scrvUSD . sfrxUSD

## Excluded (1)
reUSD -- parked: protocol migration in progress, awaiting review cycle. Not
scanned or diffed this period.

## Changed (4)

### DOLA -- touches: mint inflation + forced pause (both DE-RISKING; see Pending)
- Sole delta this period is a newly-queued critical-lever op pair (full detail in
  Pending). No role / parameter / contract / finding changes; the affected
  market's on-chain ceiling is still 500,000 DOLA (`ceilings(0xb516...330b)` ==
  5e23 at both baseline and head -- unchanged until the op executes).
  - verified: DOLA.json vs baseline struct-diff -> only pending_lever_calls +1;
    ceilings unchanged. (eth_call + struct diff, 2026-06-22)

### StakedUSDeV2 -- touches: blacklisting (end-users only) -> lender-neutral
- FULL_RESTRICTED_STAKER_ROLE +34 members (634 -> 668) -- Ethena end-user
  restriction list. 34 RoleGranted (blk 25329047-25353886), sole granter =
  existing BLACKLIST_MANAGER EOA 0x49e9...55fe; role-admin (DEFAULT_ADMIN)
  unchanged; no FiRM address among grantees; confiscation primitive not exercised.
- transferInRewards -- ~54,449 USDe reward injection @ blk 25372189 (yield-
  additive, raises sUSDe price-per-share).
  - verified: eth_getLogs RoleGranted x34 + RewardsReceived; eth_call getRoleAdmin
    unchanged. (2026-06-22)

### USDe -- touches: none (lender-neutral)
- benefactor() +1 (0xc31b...) via BenefactorAdded @ blk 25371507 -- KYC'd
  order-originator (user-tier), NOT MINTER_ROLE; minter() still EthenaMinting V2;
  rate-limit caps (200M mint / 10M redeem) unchanged.
- mint/redeem set_at bumps = routine traffic (USDT mint @25373540, USDC redeem
  @25368260).
  - verified: eth_getLogs BenefactorAdded/Mint/Redeem + eth_call minter()/
    globalConfig(). (2026-06-22)

### frxUSD -- touches: none (lender-neutral)
- Only routine ERC-4626 activity on FrxUSDCustodianUsdc (573 Deposit + 578
  Withdraw in-window; deposit set_at -> 25374006). Regression-checked: receiver
  `mintCap` flat at 25,000,000 (last week's 250x raise did NOT move again); zero
  authority events on the FrxUSD token (no minter/freezer/owner/pause change),
  zero setMintCap / setApprovedOperator.
  - verified: eth_getLogs (1151 ERC-4626 logs, 0 authority events) + eth_call
    mintCap() flat. (2026-06-22)

## Pending -- newly-queued critical-lever calls (1 new)

### DOLA -- market 0xb516...330b wind-down  [HIGH, direction: DE-RISKING]
- **`changeMarketCeiling(0xb516...330b, 0)`** queued on Compound Timelock
  0x926df14a (op_id 0x59be8d6d...), block 25372140, **callable from 2026-06-24
  09:07:35 UTC** (2d delay). Sets that market's max DOLA mint cap to 0 -- disables
  new borrows. Confirmed LIVE/queued, not executed, not cancelled.
- Co-queued in the same tx (0x547366c6...): **`pauseBorrows(true)`** on the market
  directly (op_id 0x5ac86c69...) -- pauses new borrows. Together a coherent market
  wind-down.
- Context: continues the 06-15 `Fed.contraction()` that burned 120,000 DOLA from
  this same market. Net direction is risk-reducing for DOLA lenders (caps/halts
  new exposure; no impairment of existing collateral). Review window open until
  the eta -- inspect args if a non-zero ceiling was expected.
  - verified: eth_getLogs QueueTransaction (logIndex 188 + 187) @ blk 25372140;
    queuedTransactions(op_id)=true; no Execute/Cancel. (2026-06-22)

**Known exceptions (acknowledged, not action items):** DOLA `changeMarketCeiling`
ghost -- op_id 0x4bbb3e09... (Dec-2024, eta long expired), queuedTransactions=true
but zero in-window activity.

## Critical levers -- verified stable

The unbound value/pointer sweep ran on all 4 in-roster declaring assets --
no movement this period:
- **DOLA** -- operator() pointer stable (Compound Timelock 0x926d...); the new
  changeMarketCeiling op is on a BOUND lever (ceilings), queued-not-executed.
- **crvUSD** -- execute / add_market / rug+reduce_debt_ceiling / set_monetary_policy
  all stable; totalSupply byte-identical across the window (zero net supply
  movement); all gates (dao / emergency / minter) flat.
- **rewardVault** -- setStrategy / setAllocator pointers (CurveStrategy /
  OnlyBoostAllocator) unchanged; impl bytecode codehash identical.
- **scrvUSD** -- set_accountant and set_withdraw_limit_module both still 0x0;
  custody-flow inert (totalDebt 0, empty queue).

---
trustfall-weekly @ 25d4c93 . 8 sidecars diffed vs accepted baseline 2026-06-15
(crvUSD cold-reverified) . verification: read-only on-chain claim-walk / 106 on-chain cast calls.

reUSD parked as of 2026-06-15 (protocol migration; pending review), excluded
from this run.
