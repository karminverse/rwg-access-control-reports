# Collateral Integrity Checkup — 2026-06-08

Baseline: checkpoint 0 (2026-06-05)  ·  Scanner trustfall-scanner-weekly @ 47dedf9  ·  schema_version 2
Observed 9  |  changed 5  |  no-change 4  |  new 0  |  unreadable 0
Data integrity: clean (0 ERROR, 0 WARN across roster)

## No change (4)
crvUSD · DOLA · rewardVault · scrvUSD — no role / parameter / finding / pending / authority movement since checkpoint.

## Changed (5)

### StakedUSDeV2 — touches: blacklisting
- roles ~1: `FULL_RESTRICTED_STAKER_ROLE` +18 members (now 601 restricted addresses, up from 583; restricted holders are blocked from transfers — Ethena compliance freezes).
  ↳ verified `eth_getLogs`: exactly 18 `RoleGranted` events over blocks [25247000, 25274573], granted accounts byte-identical to the sidecar diff, 0 removals. (verified_via: eth_getLogs, block 25274573)
- routine: `transferInRewards` exercised since baseline (yield injection, no value change).

### reUSD — touches: fee manipulation (oracle / share-price)
- params ~1: `setSharePrice` lever moved — `getSharePrice` 1.081404e18 → 1.082028e18 (+0.058%).
  ↳ verified `eth_call` `getSharePrice()` → 1.082028e18 @ block 25274572. Routine NAV accrual via the NAVConsumer oracle, inside the 10% deviation guard; `PRICE_SETTER_ROLE` held only by the oracle (no EOA). (verified_via: eth_call)
- routine: `deposit` exercised since baseline (no value change).

### USDe — touches: none (lender-neutral)
- roles ~1: `benefactor()` +2 members (`0x3b6aee12…`, `0xe0a6726c…`) on EthenaMinting.
  ↳ verified `eth_call` `isWhitelistedBenefactor()` == true for both + `BenefactorAdded` events @ block 25271724. Added via `addWhitelistedBenefactor` (DEFAULT_ADMIN_ROLE, Ethena 5/10 Safe). Benefactors are whitelisted collateral-source counterparties in the mint/redeem custody routing; they hold no mint / redeem / admin / pause / blacklist power, so they expand the mint-custody surface only and cannot impair value. (verified_via: eth_call + eth_getLogs)
- routine: `mint` / `redeem` exercised since baseline.

### frxUSD — touches: none (lender-neutral)
- params ~1: `deposit` exercised @ block 25274510 (`set_at_block` only, no value change).
  ↳ verified `eth_getLogs`: canonical ERC-4626 `Deposit` event @ 25274510. Routine mint-path within the unchanged 400,000,000e18 mintCap; no authority shift. (verified_via: eth_getLogs)

### sfrxUSD — touches: none (lender-neutral; scanner re-detection)
- findings: supply-authority-chain finding re-labeled "on Timelock" → "on Safe" (no role / parameter / contract delta).
  ↳ verified `eth_call`: `timelockAddress()` still resolves to the same 3/6 Safe `0x4b45d73b…` (threshold 3, 6 owners); `contracts[]`, roles, `minter()` members are byte-identical. The scanner merely switched which `minter()` reachability leg it surfaced — no authority moved. The Frax `timelockAddress()` has always pointed at a Safe directly; there was never a timelock observation buffer to lose. (verified_via: eth_call + sidecar-compare)

## Pending — newly-queued critical-lever calls (0)
No newly-queued material lever calls this period.

**Known exceptions (acknowledged, not action items):** DOLA `changeMarketCeiling` — stale Compound-Timelock ghost (queued 2024-12-16, eta expired ~18 months ago, never executed). Still pending, silent by design.

## Critical levers — verified stable
All curated critical levers across the 5 assets that declare them (DOLA · crvUSD · reUSD · rewardVault · scrvUSD) were independently verified on-chain over the checkpoint window — **including the unbound value/pointer levers the deterministic core cannot diff** (rewardVault fee percents 0.1% / 16.5%; oracle / adapter / accountant / monetary-policy pointers; Aragon `execute`). Zero unexpected firings. The only critical lever that moved is reUSD `setSharePrice` (+0.058%, routine NAV accrual, above). The 3 Curve Ownership votes that executed in-window were decoded — none touched a crvUSD supply lever (one hit an out-of-scope CurveLend controller).

---
trustfall-scanner-weekly @ 47dedf9 · schema_version 2 · 9 sidecars diffed vs accepted baseline 2026-06-05 · every CHANGED item on-chain verified
