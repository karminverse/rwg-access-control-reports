# Collateral Integrity Checkup -- 2026-07-27

Baseline accepted 2026-07-20  |  Scanner trustfall-weekly @ 412c8f5  |  schema_version 2
Observed 9  |  changed 4  |  no-change 5  |  new 0  |  unreadable 0
Data integrity: clean (0 ERROR, 0 WARN across roster).

Verification: read-only on-chain claim-walk per changed asset + unbound critical-lever
sweep, then an adversarial refutation pass on the headline items and a completeness
critic. Every CHANGED / PENDING item carries an on-chain evidence stamp.

## TL;DR

Net: **nothing lender-adverse this period -- every genuine change is de-risking or
lender-neutral.** Two items worth noting, neither an alarm: (1) two governance operations
sit ready-but-unexecuted on Ethena's 24h timelock that only REMOVE minter-side
capabilities and move roles onto the timelock -- de-risking, though they edit the
zero-delay executor whitelist we track. (2) reUSD's staked *junior* tranche (reUSDe) had
a new minter wired in by its own 2-day timelock -- fully isolated from the senior reUSD
collateral DOLA lenders hold; reUSDe supply actually contracted.

- **reUSD** -- a new MINTER on the reUSDe *junior* share token (a redemption contract,
  granted by reUSD's 2-day timelock) plus a new operator role gating a benign deposit()
  on that tranche's redemption vault. Both isolated to reUSDe; the senior reUSD
  collateral's mint authority, insurance layer and NAV feed are untouched.
  (mint inflation -- junior tranche only; lender-neutral for reUSD)
- **USDe** -- 10 user-tier benefactor whitelist additions (none can mint/redeem/administer)
  plus routine mint/redeem markers; last period's 24h-timelock ownership posture held
  (setMinter from the 5/10 Safe still reverts). (mint inflation -- lender-neutral)
- **StakedUSDeV2** -- +18 end-user blacklist entries by the unchanged manager key, zero
  FiRM addresses; the instant-blacklist bypass stays inert. (blacklisting -- lender-neutral)
- **frxUSD** -- one routine USDC-custodian deposit; mint cap, fees and proxy impl/admin all
  verified flat. (lender-neutral)
- No-change (5): crvUSD . rewardVault . scrvUSD . DOLA . sfrxUSD. Pending: two de-risking
  Ethena-timelock ops (below). Critical levers (DOLA . crvUSD . rewardVault . scrvUSD .
  reUSD): all stable.

## No change (5)
crvUSD . rewardVault . scrvUSD . DOLA . sfrxUSD

## Changed (4)

No adverse harm-vector movers this period; ordered by substance.

### reUSD -- touches: mint inflation (junior tranche only) -> LENDER-NEUTRAL for reUSD
- **Two genuine authority changes, both on the STAKED JUNIOR tranche reUSDe** -- a token
  distinct from the senior reUSD collateral DOLA lenders hold, and isolated from it
  on-chain:
  1. A **new MINTER on the reUSDe share token**, granted by reUSD's 2-day governance
     timelock (min delay 172,800s, confirmed). The grantee is a redemption
     (WindowRedemption) contract: it holds MINTER on reUSDe, holds none on reUSD, and
     holds zero reUSD. Over the window reUSDe total supply **contracted** (~1.447e25 ->
     ~1.341e25) -- the new minter has not inflated anything.
  2. A **new operator role gating deposit(address,uint256)** on that tranche's redemption
     vault, granted to an ops key at zero execution delay. The vault's source is
     unverified, so it was reverse-engineered from bytecode: deposit() hard-codes
     from = caller, to = vault -- a fund-IN primitive only. Withdraw / emergency-withdraw /
     set-authority are gated to roles this key does NOT hold. It can top up the redemption
     vault; it cannot extract or reconfigure.
- **The senior reUSD collateral is untouched:** zero role grants/revokes on the reUSD token
  over the window; its three canonical minters unchanged; the NAV feed (share price
  +0.119%, routine daily accrual) verified to the wei; separate insurance layer and
  custodian. No path from the reUSDe minter reaches reUSD value -- it would first need a
  new, separately-visible role grant on the reUSD token, of which there were none.
  - verified: eth_getLogs RoleGranted(MINTER) on reUSDe @ block 25,582,130 (granter = the
    2-day timelock, getMinDelay 172800); RoleGranted/RoleRevoked on the reUSD token over
    the window = 0; reUSDe totalSupply + reUSD share price at both endpoints; AccessManager
    role/target enumeration (one new binding, selector 0x47e7ef24 = deposit, no second
    binding). Adversarially re-derived from bytecode. (eth_call + eth_getLogs, 2026-07-27)

### USDe -- touches: mint inflation (governance) -> LENDER-NEUTRAL; migration posture held
- **10 addresses added to the benefactor whitelist** on the minter contract, via the
  intended zero-delay benefactor path; each verified to hold NEITHER minter, redeemer, nor
  admin authority -- user-tier counterparties, not mint authority. Routine mint/redeem
  activity markers otherwise.
- **Last period's timelock-ownership migration held with no regression:** USDe owner() is
  the 24h timelock, pendingOwner() is zero, and a simulated setMinter from the 5/10 Safe
  reverts on the ownership check; setMinter remains off the timelock's zero-delay whitelist.
  - verified: eth_getLogs 10 benefactor-add events over the window, hasRole(minter/redeemer/
    admin, each) = false; eth_call owner()/pendingOwner()/minter(); setMinter
    revert-simulation from the 5/10 Safe. (eth_call + eth_getLogs, 2026-07-27)
- Two ready-but-unexecuted Ethena-timelock ops that further de-risk this minter are in
  Pending, below.

### StakedUSDeV2 -- touches: blacklisting (end-user) -> LENDER-NEUTRAL
- **+18 wallets added to the sUSDe restricted/seizure tier** over the window, all by the
  existing, unchanged blacklist-manager key. All 18 are end-user externally-owned accounts;
  none is a FiRM market or escrow -- no lender collateral restricted. (On-chain there were
  19 grants; one was granted and removed within the same window, netting 18.)
- **Posture held:** sUSDe owner() and admin remain the 24h timelock (the same migration as
  USDe); the instant-blacklist zero-delay bypass stays inert -- the timelock does not hold
  the blacklist-manager role, the 5/10 Safe holds it directly.
  - verified: eth_getLogs 19 RoleGranted / 1 in-window RoleRevoked, granter holds the
    blacklist-manager role; code() per address (all EOAs); hasRole posture checks.
    (eth_call + eth_getLogs, 2026-07-27)

### frxUSD -- touches: none (lender-neutral)
- One **routine ERC-4626 deposit** on the USDC custodian (the deposit genuinely occurred).
  All harm-vector levers verified flat: mint cap identical at both window endpoints,
  mint/redeem fees unchanged, EIP-1967 impl + admin slots byte-identical, zero authority
  events on the token or custodian. No mint-inflation movement.
  - verified: eth_call cap/fee getters + impl/admin storage slots at both endpoints;
    eth_getLogs authority events = 0. (2026-07-27)

## Pending -- Ethena-timelock governance ops (2, de-risking)

Two operations sit scheduled and **READY** (their 24h delay elapsed 2026-07-24) but
**UNEXECUTED** on Ethena's governance timelock -- the shared 24h governor of USDe and
sUSDe. Both are governance-hardening / de-risking; neither opens a supply, blacklist,
upgrade, fee, or pause path over the USDe/sUSDe backing that collateralizes DOLA:

- **Op A (7 calls):** removes two mint-benefactors from the USDe minter and adds five
  zero-delay executor-whitelist entries -- every added selector is a capability REMOVAL
  (remove-minter-role, remove-redeemer-role, remove-collateral-manager-role,
  remove-whitelisted-benefactor, revoke-role). The whitelist grows only with
  revoke/remove power, never grant power.
- **Op B (13 calls):** migrates six admin roles from the 5/10 Safe to the 24h timelock on
  a SEPARATE Ethena "USDtb" subsystem contract -- a distinct product, not the USDe/sUSDe
  mint path; it holds no authority over USDe or sUSDe and holds zero of either token. The
  effect is to add delay to that contract's governance.
- Neither op touches the USDe cross-chain lockbox adapter (~1.56B USDe): its rate-limiter
  is still absent and no peer re-pointing occurred this window -- unchanged; the standing
  watch on that lever persists.
  - verified: eth_getLogs 20 CallScheduled across the two ops (blocks 25,595,578 /
    25,595,580); each op isOperationReady = true, isOperationDone = false, 0 CallExecuted;
    every scheduled selector decoded (all removal/revoke/migration); adapter rate-limiter =
    zero-address and 0 peer-set events at both endpoints. (eth_call + eth_getLogs, 2026-07-27)

Framing: these are visible now and executable at any time (these timelock ops do not
expire). They reduce, not expand, the mint-authority attack surface -- worth watching for
execution next period, not an alarm. No other newly-queued critical-lever calls anywhere
in the roster this period.

## Critical levers -- verified stable

The unbound value/pointer lever sweep ran on all 5 in-roster declaring assets -- no
movement this period:
- **DOLA** -- operator pointer = the 2-day governance timelock, pending-operator zero; the
  four Feds' chair/governor identical at both endpoints; total supply byte-identical (zero
  net expansion). SaveDola market still 4.9M drawn of a 5.0M cap (98%), ceiling not raised
  and no ceiling-raise queued.
- **crvUSD** -- monetary-policy pointers byte-identical across all controllers; zero setter
  events; total supply flat (indirect proof for the event-less fee-mint lever).
- **rewardVault** -- strategy and allocator pointers identical at both endpoints; root-vault
  codehash byte-identical; component-setter events zero across the window.
- **scrvUSD** -- accountant and withdraw-limit-module both still zero-address; custody flow
  inert.
- **reUSD** -- all declared unbound value/pointer levers stable, each closed by both a
  setter-event sweep and two-endpoint getter reads (the new reUSDe junior-tranche roles are
  surfaced in Changed, above).

---
trustfall-weekly @ 412c8f5 . 9 sidecars diffed vs accepted baseline 2026-07-20 .
verification: 10 read-only claim-walk agents + a 5-agent adversarial refutation / critic
pass + 1 pending-batch claim-walk (cast eth_call / eth_getLogs / call simulation /
bytecode trace). Toolchain: pip-audit clean.
