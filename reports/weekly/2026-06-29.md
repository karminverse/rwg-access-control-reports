# Collateral Integrity Checkup -- 2026-06-29

Baseline accepted 2026-06-22  |  Scanner trustfall-weekly @ 13d0ea7  |  schema_version 2
Observed 8  |  changed 6  |  no-change 2  |  new 0  |  unreadable 0  |  excluded 1
Data integrity: clean (0 ERROR, 0 WARN across roster).

Verification: read-only on-chain claim-walk per changed asset + critical-lever
sweep (10 agents). Every CHANGED / PENDING item carries an on-chain evidence stamp.

## TL;DR

Net: a **de-risking week -- zero risk-increasing changes anywhere.** The headline
is DOLA's INV-market wind-down EXECUTING (last week's queued `changeMarketCeiling
-> 0` plus a co-queued borrow pause, both consummated). Two governance hardenings
(DOLA, rewardVault); the rest routine or no-change.

- **DOLA** -- INV market wound down: `changeMarketCeiling` 500k -> 0 AND
  `pauseBorrows(true)` both **EXECUTED** (de-risking; completes the 06-15 contraction).
- **rewardVault** -- ProtocolTimelock hardened: the 3/5 Safe renounced
  DEFAULT_ADMIN (timelock now self-admin) + a 2/5 sibling Safe added as CANCELLER.
  De-risking; reaches no collateral lever.
- **USDe** -- benefactor churn +4 / -3 (the 3 removed = coordinated Ethena
  de-auth, also blacklisted on sUSDe); user-tier, minter unchanged -> lender-neutral.
- **StakedUSDeV2** -- +63 end-user restrictions + reward stream -> lender-neutral.
- **frxUSD** -- routine user deposits (one dormant custodian reactivated by a
  ~3.3 frxUSD deposit); mintCap flat -> lender-neutral.
- No-change (2): crvUSD . scrvUSD. Excluded (1): reUSD (parked). Pending: none new.
  Critical levers: all stable.

## No change (2)
crvUSD . scrvUSD

## Excluded (1)
reUSD -- parked: protocol migration in progress, awaiting /debrief cycle. Not
scanned or diffed this period.

## Changed (6)

Harm-vector movers first (all de-risking this week), then the lender-neutral.

### DOLA -- touches: mint inflation + forced pause (both EXECUTED, DE-RISKING)
- **INV market wind-down consummated.** Last week's queued ops both EXECUTED in a
  single tx (block 25386485): `changeMarketCeiling(INV market, 0)` -- the per-market
  DOLA mint cap went 500,000 -> 0 -- and `pauseBorrows(true)` -- new borrows on the
  INV market halted. No new DOLA can be issued against INV collateral; existing
  collateral value is unaffected. Completes the wind-down begun 06-15 (120k-DOLA
  contraction on the same market).
  - verified: eth_call ceilings(INV)=0 (was 5e23) + borrowPaused()=true; eth_getLogs
    ExecuteTransaction for both op-ids @ block 25386485, queue cleared, no Cancel.
    (2026-06-29)
- Nothing else moved: Fed global supplyCeiling (100B), operator(), and the 5-Fed
  minter set all unchanged.

### rewardVault -- touches: governance (proxy-upgrade-adjacent) -> DE-RISKING
- **ProtocolTimelock admin hardened** (one tx, block 25395427): the Stake DAO 3/5
  Safe **renounced DEFAULT_ADMIN_ROLE** (self-revoke), leaving the TimelockController
  as its own sole admin -- so future role changes must traverse the 2-day timelock
  rather than a one-tx Safe action. A **2/5 sibling Safe** (same 5 Stake DAO
  signers, Safe v1.4.1) was **added as CANCELLER** (can only cancel queued ops -- a
  defensive primitive).
  - verified: eth_getLogs RoleRevoked(DEFAULT_ADMIN, 3/5 Safe; account==sender) +
    RoleGranted(CANCELLER, 2/5 sibling Safe); eth_call confirms timelock = sole
    DEFAULT_ADMIN, identical 5-owner sets. (2026-06-29)
- Neither change reaches any DOLA-collateral-impairing lever (setStrategy /
  setAllocator / pause / shutdown remain 2d-timelock-gated).

### USDe -- touches: none (lender-neutral)
- benefactor() roster +4 / -3 on EthenaMinting. benefactor is a user-tier KYC'd
  order-originator, NOT a minter; minter() unchanged (EthenaMinting V2). The **3
  removed are the same addresses blacklisted on sUSDe this week** -- a coordinated
  Ethena de-authorization (de-risking). Routine mint/redeem activity otherwise.
  - verified: eth_getLogs BenefactorAdded x4 / BenefactorRemoved x3 + eth_call
    isWhitelistedBenefactor flips, removed hold no MINTER_ROLE; minter() unchanged.
    (2026-06-29)

### StakedUSDeV2 -- touches: blacklisting (end-users only) -> lender-neutral
- FULL_RESTRICTED_STAKER_ROLE +63 end-user members (granter = existing
  blacklist-manager; role admin unchanged; no FiRM address restricted; confiscation
  primitive not exercised). transferInRewards = 22 reward injections (~57,612 USDe
  last @ block 25424788), yield-additive (sUSDe appreciates).
  - verified: eth_getLogs RoleGranted x63 + RewardsReceived; eth_call getRoleAdmin
    unchanged. (2026-06-29)

### frxUSD -- touches: none (lender-neutral)
- Routine ERC-4626 user deposits: FrxUSDCustodianUsdc activity, plus a dormant
  custodian (FrxUSDCustodian) reactivated by a single ~3.3 frxUSD self-deposit.
  Regression-checked: receiver mintCap flat at 25,000,000 (no new raise); zero
  authority events (minter / freezer / owner / pause / cap / fee) on the token and
  all 3 custodians.
  - verified: eth_getLogs Deposit events + zero authority events; eth_call mintCap()
    unchanged. (2026-06-29)

### sfrxUSD -- no real change (scanner label artifact)
- Core-flagged a finding re-label (supply-authority chain Safe -> Timelock), but
  on-chain control is unchanged -- the same 3-of-N Safe, zero rotation events: a
  scanner re-classification, not a transfer. No collateral impact.
  (verified eth_call + eth_getLogs, 2026-06-29)

## Pending -- newly-queued critical-lever calls (0 new)

No newly-queued critical-lever timelock calls this period. **One consummated:**
DOLA's 06-22 `changeMarketCeiling -> 0` (+ co-queued `pauseBorrows`) **EXECUTED**
this week -- surfaced above under Changed -> DOLA.

**Known exceptions (acknowledged, not action items):** DOLA `changeMarketCeiling`
ghost -- the Dec-2024 expired op, still queued but unexecutable (past grace), no
in-window activity. See the acknowledged-exceptions registry.

## Critical levers -- verified stable

The unbound value/pointer lever sweep ran on all 4 in-roster declaring assets --
no movement this period:
- **DOLA** -- operator() pointer stable (Compound Timelock); the executed
  changeMarketCeiling is a tracked (bound) lever, already in Changed.
- **crvUSD** -- set_monetary_policy unchanged across all 9 controllers; totalSupply
  byte-identical (zero net supply movement); all gates flat.
- **rewardVault** -- setStrategy / setAllocator pointers unchanged; implementation
  non-upgradeable (codehash identical). The timelock role change above is a
  governance delta, not an unbound-lever move.
- **scrvUSD** -- set_accountant and set_withdraw_limit_module both still 0x0;
  custody-flow inert (totalDebt 0, empty queue).

---
trustfall-weekly @ 13d0ea7 . 8 sidecars diffed vs accepted baseline 2026-06-22 .
verification: 10 read-only agents / 122 on-chain cast calls.
reUSD parked (protocol migration; awaiting /debrief), excluded from this run via
the checkup exclusion list.
Toolchain: pip-audit clean (aiohttp 3.14.1 bump held).
